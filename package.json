{
  "name": "product-management-alx",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n API,WEB -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:build": "concurrently -n API,WEB -c blue,green \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "start": "concurrently -n API,WEB -c blue,green \"npm --prefix backend start\" \"npm --prefix frontend run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
